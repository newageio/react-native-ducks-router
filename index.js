import Router from './src/RouterComponent';
import routerReducer, { actionCreators, actionTypes, initialState, selectors } from './src/redux';

export {
  routerReducer,
  actionCreators,
  actionTypes,
  initialState,
  selectors,
};

export default Router;
